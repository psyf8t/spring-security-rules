# Same file with gateway config + broad expose. Gateway rule fires
# starting at the spring.cloud.gateway key.
# ruleid: spring-cloud-gateway-actuator-exposed
spring.cloud.gateway.routes[0].id=foo
# ruleid: spring-actuator-broadly-exposed
management.endpoints.web.exposure.include=*
